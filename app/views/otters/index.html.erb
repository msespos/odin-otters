<h2>Meet the Otters!</h2>
<% @otters.each_with_index do |otter, index| %>
  <br><%= link_to 'Delete', otter, method: :delete %>
  Otter #<%= index + 1 %>:
  <%= link_to otter.name, otter_path(otter.id) %>
<% end %><br><br>

<%= link_to 'Create a new Otter!', new_otter_path %>